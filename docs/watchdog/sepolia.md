# Watchdog on Sepolia

This page moved to **[`operator-deployment.md`](operator-deployment.md)**.

Sepolia uses the same operator model as mainnet (internal snapshot API, live L1, per-deployment addresses, persistent checkpoints). That document is the production-like runbook; [`getting-started.md`](getting-started.md) remains for local Anvil + `sequencer-devnet` only.
